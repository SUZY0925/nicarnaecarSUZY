<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <!--   eemail,ename,ebirth;
     ephone;
     eaddress;
    edate;
     eoffice;
     eposition;
     estate; -->
<mapper namespace="com.prj.nicarnaecar.dao.ELoginDAO">
   <insert id="EInsert" parameterType="EmployeeVO">
      insert into employee(eemail,epassword,ename,ebirth,ephone,eaddress,eoffice,eposition)
      values(#{eemail},#{epassword},#{ename},#{ebirth},#{ephone},#{eaddress},#{eoffice},#{eposition})
   </insert>
   
   <select id="EgetMember" resultType="EmployeeVO">
      select * from employee where eemail=#{eemail}   
   </select>
   
<!--    <update id="EUpdate">
      update employee set
         epassword    = #{epassword},
         eaddress    = #{eaddress},
         ephone       = #{ephone}
      where eemail    = #{eemail}
   </update> -->
   
   <update id="EUpdate">
      update employee set
         ephone    = #{ephone},
         eaddress    = #{eaddress},
         eoffice    = #{eoffice},
         eposition  = #{eposition},
         estate    = #{estate}
      where eemail    = #{eemail}
   </update>
   
   <select id="Efindid">
      select eemail from employee where ename = #{ename} and ephone = #{ephone}
   </select>
   
   <select id="Efindpw">
      select epassword from employee where eemail = #{eemail} and ebirth = #{ebirth}
   </select>
   
    <select id="EmemberList" resultType="EmployeeVO">
      select eoffice,ename,eposition,estate,eemail,ephone,ebirth,edate from employee order by eoffice, decode(eposition,'소장','대리','사원')
   </select>
</mapper>